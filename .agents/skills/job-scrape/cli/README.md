# Job Scrape CLI

Cross-source orchestration for repository job portal skills. It normalizes
adapter output, verifies public details, deduplicates listings, reconciles
private state, and renders source coverage. See the parent `SKILL.md` for safe
usage.
