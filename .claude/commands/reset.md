# /reset - Reset Candidate Profile Data

Claude compatibility wrapper.

Canonical workflow guidance now lives in:

- `agent-guidance/project-guide.md`
- `agent-guidance/workflows/job-reset.md`

When `/reset` is invoked, read those files first and follow the shared workflow. Preserve the legacy behavior: show exactly what will be cleared, require explicit confirmation, and preserve framework structure.
