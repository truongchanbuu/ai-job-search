# /setup - Profile Onboarding

Claude compatibility wrapper.

Canonical workflow guidance now lives in:

- `agent-guidance/project-guide.md`
- `agent-guidance/workflows/job-setup.md`
- `agent-guidance/job-application-assistant/`

When `/setup` is invoked, read those files first and follow the shared workflow. Preserve the legacy behavior: offer documents folder, single CV import, and interview mode; update profile guidance only from supported facts; surface conflicts before writing; and keep personal output in gitignored paths.
