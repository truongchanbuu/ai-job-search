# /rank - Triage Scraped Jobs into a Ranked Shortlist

Claude compatibility wrapper.

Canonical workflow guidance now lives in:

- `agent-guidance/project-guide.md`
- `agent-guidance/workflows/job-rank.md`
- `agent-guidance/job-application-assistant/04-job-evaluation.md`

When `/rank` is invoked, read those files first and follow the shared workflow. Preserve the legacy behavior: ranking is triage only, expired postings are not scored from title alone, and `/apply` remains the authoritative evaluation.
