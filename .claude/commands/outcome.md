# /outcome - Record the Result of an Application

Claude compatibility wrapper.

Canonical workflow guidance now lives in:

- `agent-guidance/project-guide.md`
- `agent-guidance/workflows/job-outcome.md`

When `/outcome` is invoked, read those files first and follow the shared workflow. Preserve the legacy behavior: record what happened, archive submitted materials and posting context, update the tracker, and leave framework recalibration to setup.
