# /add-portal - Generate a Job-Portal Search Skill for Your Local Market

Claude compatibility wrapper.

Canonical workflow guidance now lives in:

- `agent-guidance/project-guide.md`
- `agent-guidance/workflows/job-add-portal.md`
- `.agents/skills/*-search`

When `/add-portal` is invoked, read those files first and follow the shared workflow. Preserve the legacy behavior: investigate the portal, scaffold a CLI-backed skill, test a live query, and register it only when useful.
