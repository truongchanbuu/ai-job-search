# Cover Letter Templates and Tailoring Guide

Default cover letter output lives under `cover_letters/`.

Rules:

- Compile with `xelatex`.
- Target exactly one page.
- Address the correct recipient when known.
- Verify company claims before using them.
- Keep bullet/list formatting compatible with the active template.
