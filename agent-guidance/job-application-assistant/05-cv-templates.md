# CV Templates and Tailoring Guide

Default CV output lives under `cv/`.

Rules:

- Compile with `lualatex`.
- Target exactly two pages unless an active custom template says otherwise.
- Avoid orphaned section titles and broken page flow.
- Preserve ATS-readable contact details.
- Use `pdftotext -layout` when available to verify text extraction and reading order.
